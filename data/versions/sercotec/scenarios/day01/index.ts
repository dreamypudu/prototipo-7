import type { MeetingSequence, ScenarioNode } from '../../../../../types';

// Sin escenarios definidos todavía. Agrega modulos por día cuando exista contenido.
export const nodes: ScenarioNode[] = [];
export const sequences: MeetingSequence[] = [];
