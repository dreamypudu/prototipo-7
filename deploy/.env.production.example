# Copia este archivo a `.env.production` y completa los valores.
# Nunca commitees el .env.production con credenciales reales.

# Base de datos Postgres dentro del docker-compose.
# El backend construye DATABASE_URL internamente apuntando al servicio `db`.
POSTGRES_USER=compass
POSTGRES_PASSWORD=cambiar-esto-por-una-clave-larga
POSTGRES_DB=compass
POSTGRES_BIND=127.0.0.1
POSTGRES_PORT=8000

# Dominios permitidos por CORS, separados por coma. Sin slash final.
ALLOWED_ORIGINS=https://compass.giia.udec.cl

# Autenticacion de plataforma. Genera AUTH_JWT_SECRET_KEY con una clave larga
# y aleatoria (minimo 32 caracteres). La cuenta seed inicial queda con role=user.
APP_ENV=production
AUTH_JWT_SECRET_KEY=cambiar-esto-por-una-clave-aleatoria-muy-larga
AUTH_ACCESS_TOKEN_MINUTES=15
AUTH_REFRESH_TOKEN_HOURS=12
AUTH_COOKIE_SECURE=true
AUTH_COOKIE_SAMESITE=strict
AUTH_SEED_EMAIL=lab@compass.local
AUTH_SEED_PASSWORD=cambiar-esto-por-una-clave-larga
AUTH_SEED_ROLE=user
AUTH_SEED_FULL_NAME=Cuenta laboratorio
AUTH_SEED_OVERWRITE=false

# Puertos en los que estarán alojados los servicios.
# Backend y Postgres quedan solo en localhost; frontend queda publico.
BACKEND_BIND=127.0.0.1
BACKEND_PORT=8001
FRONTEND_PORT=8002

# URL que el frontend usa para hablar con el backend.
# Si front y back comparten dominio (recomendado), deja /api (ruta relativa).
# Si quedan en subdominios distintos: https://api.compass.giia.udec.cl
VITE_API_URL=/api
